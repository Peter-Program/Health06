<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorMainPrime</item>
        <item name="colorPrimaryDark">@color/colorMainPrimeDark</item>
        <item name="colorAccent">@color/colorMainAccent</item>
        <item name="android:colorBackground">@color/colorBackground</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/colorMainPrime</item>
    </style>

    <style name="NutritionTheme" parent="Theme.AppCompat">
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="colorPrimary">@color/colorNutrition</item>
        <item name="colorAccent">@color/colorNutrition</item>
        <item name="android:textColor">@color/colorText</item>
    </style>

    <style name="WorkoutTheme" parent="Theme.AppCompat">
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="colorPrimary">@color/colorWorkout</item>
        <item name="colorAccent">@color/colorWorkout</item>
    </style>

</resources>
